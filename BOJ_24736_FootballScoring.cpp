#include <iostream>
using namespace std;

int main(void) {
    int t, f, s, p, c;
    cin >> t >> f >> s >> p >> c;
    cout << 6*t + 3*f + 2*s + p + 2*c << endl;
    cin >> t >> f >> s >> p >> c;
    cout << 6*t + 3*f + 2*s + p + 2*c << endl;

    return 0;
}
